#include <stdio.h>

//main method that calls other methods. The method is key sensitive, so Main != main.
//this method is invoked every time the program starts, this explains why just one main can be called on startup. 

int main(void) 
{
	//1.1

	//helloWorldEdX();
	//printMultipleLines();
	//multipleLinesOnePrintf();
	//quoteOnComment();

	//1.2

	//firstFor();
	//multipleFor();
	//printInteger();
	//Balance();
	//CollectExercise();
	//PowarBasis();
	//ReadInput();
	//MultipleScan();
	//ScanOnLoop();
	//RobotPowerCalculator();

	//1.3
	//PrintChar();
	ReadCharUser();
	return 0;
}